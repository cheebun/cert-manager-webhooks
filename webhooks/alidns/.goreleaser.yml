version: 2

project_name: alidns-webhook

builds:
  - id: alidns-webhook
    binary: webhook
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -extldflags "-static"
    flags:
      - -trimpath

dockers:
  - id: alidns-webhook-amd64
    ids:
      - alidns-webhook
    use: buildx
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile
    image_templates:
      - ghcr.io/{{ .Env.GITHUB_REPOSITORY }}/alidns:{{ .Tag }}-amd64
    build_flag_templates:
      - --platform=linux/amd64

  - id: alidns-webhook-arm64
    ids:
      - alidns-webhook
    use: buildx
    goos: linux
    goarch: arm64
    dockerfile: Dockerfile
    image_templates:
      - ghcr.io/{{ .Env.GITHUB_REPOSITORY }}/alidns:{{ .Tag }}-arm64
    build_flag_templates:
      - --platform=linux/arm64

docker_manifests:
  - name_template: ghcr.io/{{ .Env.GITHUB_REPOSITORY }}/alidns:{{ .Tag }}
    image_templates:
      - ghcr.io/{{ .Env.GITHUB_REPOSITORY }}/alidns:{{ .Tag }}-amd64
      - ghcr.io/{{ .Env.GITHUB_REPOSITORY }}/alidns:{{ .Tag }}-arm64

  - name_template: ghcr.io/{{ .Env.GITHUB_REPOSITORY }}/alidns:latest
    image_templates:
      - ghcr.io/{{ .Env.GITHUB_REPOSITORY }}/alidns:{{ .Tag }}-amd64
      - ghcr.io/{{ .Env.GITHUB_REPOSITORY }}/alidns:{{ .Tag }}-arm64

archives:
  - id: alidns-webhook
    format: tar.gz
    name_template: "alidns-webhook_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "alidns-webhook_{{ .Version }}_checksums.txt"

release:
  disable: true

changelog:
  disable: true
