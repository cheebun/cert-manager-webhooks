apiVersion: v1
kind: Service
metadata:
  name: {{ include "tencent-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "tencent-webhook.name" . }}
    chart: {{ include "tencent-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "tencent-webhook.name" . }}
    release: {{ .Release.Name }}
